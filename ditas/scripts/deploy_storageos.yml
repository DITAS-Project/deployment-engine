---
  - hosts: [master]
    become: yes
    tasks:
      - name: Add StorageOS repository
        shell: helm repo add storageos https://charts.storageos.com
      - name: Update Helm repositories
        shell: helm repo update
      - name: Deploy StorageOS
        shell: helm install storageos/storageoscluster-operator --namespace storageos-operator
        