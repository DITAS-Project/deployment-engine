apiVersion: v1
kind: Service
metadata:
  name: "vdm-{{ vdmName }}"
spec:
  type: NodePort
  selector:
    component: "vdm-{{ vdmName }}"
  ports:
  - name: http
    port: 8080
    targetPort: 50003

---
apiVersion: v1
kind: Pod
metadata:
  name: "ds4m-{{ vdmName }}"
  labels:
    component: "vdm-{{ vdmName }}"
spec:
  containers:
  - name: ds4m
    image: ditas/decision-system-for-data-and-computation-movement
    ports:
    - containerPort: 8080
    volumeMounts:
    - name: blueprint
      mountPath: /opt/blueprint

  volumes:
  - name: blueprint
    hostPath:
        path: "/home/cloudsigma/blueprint_{{ blueprintName }}"
        type: Directory
